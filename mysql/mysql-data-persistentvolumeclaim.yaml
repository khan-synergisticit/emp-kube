apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: mysql-data
  name: mysql-data
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
